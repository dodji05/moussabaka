{% extends 'base.html.twig' %}

{% block body %}

    <div class="col-md-8 offset-md-2">
        <div class="card card-outline card-success">
            <div class="card-header">
                <div class="card-title"> Ajouter un nouveau membre du jury</div>
            </div>
            <div class="card-body">
                {{ include('jury/_form.html.twig') }}
            </div>
        </div>

    </div>


    {#    <h1>Create new Candidat</h1>#}

    {#   #}

    {#    <a href="{{ path('app_candidat_index') }}">back to list</a>#}
{% endblock %}
