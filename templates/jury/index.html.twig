{% extends 'base_admin_table.html.twig' %}
{% block ti %} Liste des jury {% endblock %}
    {% block tablecontent %}
    <table class="table table-striped">
        <thead>
            <tr>
                <th></th>
                <th>NumeroJury</th>
                <th>Nom</th>
                <th>Prenom</th>

{#                <th>Active</th>#}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for jury in juries %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ jury.numeroJury }}</td>
                <td>{{ jury.nom }}</td>
                <td>{{ jury.prenom }}</td>


                <td>
                    <a href="{{ path('app_jury_show', {'id': jury.id}) }}"> <i class="fas fa-eye" style="color: #28a745">

                        </i></a>
                    <a href="{{ path('app_jury_edit', {'id': jury.id}) }}">
                        <i class="fas fa-edit" style="color: #007bff">

                        </i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{#    <a href="{{ path('app_jury_new') }}">Create new</a>#}
{% endblock %}
