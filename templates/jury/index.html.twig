{% extends 'base_table_admilte.html.twig' %}
{% block title %}Liste des Jury{% endblock %}
{% block card_header_title %} Liste des Jury{% endblock %}

{% block tablecontent %}
        <thead>
        <tr>

            <th>N°</th>
            <th>Mom</th>
            <th>Prenom</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for jury in juries %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ jury.membres.nom }}</td>
                <td>{{ jury.membres.prenoms }}</td>

                {#                    <td>{{ candidat.active ? 'Yes' : 'No' }}</td>#}
                {#                    <td>{{ candidat.createdAt ? candidat.createdAt|date('Y-m-d H:i:s') : '' }}</td>#}
                <td>
                    <a href="{{ path('app_jury_show', {'id': jury.id}) }}">
                        <i class="fas fa-eye" style="color: #28a745">

                        </i>
                    </a>
                    <a href="{{ path('app_jury_edit', {'id': jury.id}) }}">
                        <i class="fas fa-edit" style="color: #007bff">

                        </i>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>


{% endblock %}

{#{% block body %}#}
{#    <h1>Jury index</h1>#}

{#    <table class="table">#}
{#        <thead>#}
{#            <tr>#}
{#                <th>Id</th>#}
{#                <th>actions</th>#}
{#            </tr>#}
{#        </thead>#}
{#        <tbody>#}
{#        {% for jury in juries %}#}
{#            <tr>#}
{#                <td>{{ jury.id }}</td>#}
{#                <td>#}
{#                    <a href="{{ path('app_jury_show', {'id': jury.id}) }}">show</a>#}
{#                    <a href="{{ path('app_jury_edit', {'id': jury.id}) }}">edit</a>#}
{#                </td>#}
{#            </tr>#}
{#        {% else %}#}
{#            <tr>#}
{#                <td colspan="2">no records found</td>#}
{#            </tr>#}
{#        {% endfor %}#}
{#        </tbody>#}
{#    </table>#}

{#    <a href="{{ path('app_jury_new') }}">Create new</a>#}
{#{% endblock %}#}
