{% extends 'base.html.twig' %}

{% block title %}EcoledeProvenance{% endblock %}

{% block body %}
    <h1>EcoledeProvenance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ecolede_provenance.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ecolede_provenance.nom }}</td>
            </tr>
            <tr>
                <th>Localite</th>
                <td>{{ ecolede_provenance.localite }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ ecolede_provenance.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ ecolede_provenance.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ecolede_provenance_index') }}">back to list</a>

    <a href="{{ path('app_ecolede_provenance_edit', {'id': ecolede_provenance.id}) }}">edit</a>

    {{ include('ecolede_provenance/_delete_form.html.twig') }}
{% endblock %}
