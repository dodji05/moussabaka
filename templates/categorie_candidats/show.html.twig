{% extends 'base.html.twig' %}

{% block title %}CategorieCandidats{% endblock %}

{% block body %}
    <h1>CategorieCandidats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_candidat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie_candidat.name }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ categorie_candidat.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_candidats_index') }}">back to list</a>

    <a href="{{ path('app_categorie_candidats_edit', {'id': categorie_candidat.id}) }}">edit</a>

    {{ include('categorie_candidats/_delete_form.html.twig') }}
{% endblock %}
