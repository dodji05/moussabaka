<form method="post" action="{{ path('app_categorie_candidats_delete', {'id': categorie_candidat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_candidat.id) }}">
    <button class="btn">Delete</button>
</form>
