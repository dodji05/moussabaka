{{ form_start(form,{'attr':{'id':'categoriecircuits' }}) }}
    {{ form_widget(form) }}
<button class="btn btn-primary float-lg-right">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
